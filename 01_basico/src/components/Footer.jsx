import React from 'react'

const Footer = (props) => {
    return (
        <footer>
            <p>Todos direitos reservados &copy; { props.date} </p>
             
        </footer>
    );
}

export default Footer
